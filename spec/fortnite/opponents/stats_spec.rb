RSpec.describe Fortnite::Opponents::Stats do
  it "has a version number" do
    expect(Fortnite::Opponents::Stats::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
